import React from 'react';

const CourseReview = (course) => {

    console.log(course);

    return (
        <div>
            <h1>Course Review</h1>
        </div>
    );
};

export default CourseReview;
